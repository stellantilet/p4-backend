export enum EventStatusEnum {
  Scheduled = 'scheduled',
  Live = 'live',
  Ended = 'ended',
  Postponed = 'postphoned',
}

export enum ItemAttributeDisplayEnum {
  BoostNumber = 'boost_number',
  BoostPercentage = 'boost_percentage',
  Number = 'number',
  Date = 'date',
}

export enum OfferSaleEnum {
  Auction = 'auction',
  Sell = 'sell',
}

export enum OfferStatusEnum {
  Opened = 'opened',
  Closed = 'closed',
  Cancelled = 'cancelled',
}

export enum BidStatusEnum {
  Following = 'following',
  Leading = 'leading',
  Withdrawn = 'withdrawn',
  Owned = 'owned',
}
